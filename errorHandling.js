/*
JavaScript has built-in error types (TypeError, RangeError, etc.) 
and lets you create custom errors by extending the Error class.  
Use finally for cleanup that must always run (like closing connections.)
*/

//Custom Error class
